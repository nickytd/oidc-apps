# SPDX-FileCopyrightText: 2026 nickytd
# SPDX-License-Identifier: Apache-2.0

Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To verify the installation:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "oidc-apps.name" . }}

To check the controller logs:

  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "oidc-apps.name" . }} -f

Configuration:
  - Webhook port: {{ .Values.service.port }}
  - Metrics enabled: {{ .Values.metrics.enableScraping }}
{{- if .Values.metrics.enableScraping }}
  - Metrics port: {{ .Values.metrics.port }}
{{- end }}
  - VPA enabled: {{ .Values.vpa.enabled }}
  - HTTPRoute support: {{ if and .Values.global .Values.global.gateway .Values.global.gateway.httpRoutes }}{{ .Values.global.gateway.httpRoutes.enabled }}{{ else }}false{{ end }}
  - Managed Gateway: {{ if and .Values.global .Values.global.gateway }}{{ .Values.global.gateway.managed }}{{ else }}false{{ end }}
  - cert-manager integration: {{ .Values.certificate.create }}

{{- if and .Values.global .Values.global.gateway .Values.global.gateway.httpRoutes .Values.global.gateway.httpRoutes.enabled }}

HTTPRoute Support:
  Gateway API HTTPRoute support is enabled. Ensure that Gateway API CRDs are
  installed in your cluster before creating targets with httpRoute.create: true.

  To install Gateway API CRDs:
    kubectl apply -f https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.0.0/standard-install.yaml
{{- end }}

{{- if not .Values.certificate.create }}

Certificate Management:
  cert-manager integration is disabled. The controller will manage its own
  webhook certificates. Certificates are stored in memory and rotated automatically.
{{- end }}

For more information, visit:
  https://github.com/nickytd/oidc-apps-controller
